# implementation_plan.md



## 项目总目标
构建《星绪森林》0-1 阶段可落地的现代情绪冒险小游戏。MVP 不做大地图的含义是不做多个主世界场景地图，但要在一个可持续摆放元素的单主地图内形成完整玩法闭环，并能扩展新资源、新建设物、新小游戏入口和新场景边界。当前包含两个桌面原生发布目标和一个网页概念交互演示：
- 桌面原生版本 1：`Unity Windows x64`。
- 桌面原生版本 2：`Unity macOS Universal`。
- 网页概念演示：`HTML + CSS + JavaScript`，放在 Unity 核心系统和资源设计对齐之后更新，用于最终对照展示画面、交互路径和状态变化。



## 当前阶段评估
当前项目处于创意和规划阶段。已有基础文档和网页演示，但之前设计偏粗，需要补齐用户路径、状态流转、接口、数据结构、视觉对标和可实现标准。本轮目标不是把游戏做大，而是把第一轮闭环做到能指导实现。



## 文档归档要求
- 游戏设计与规划统一放入 `doc/`。
- `doc/design_document.md`：完整设计规划。
- `doc/level_sketches.md`：关卡草图与节点数据变化。
- `doc/interaction_technical_spec.md`：玩家行为路径、状态变化、接口和 Unity 拆分。
- `doc/visual_alignment_standard.md`：画面对齐方法、80 分通过标准和三轮 review。
- `PROJECT_ARCHITECTURE_UPDATE.md`：v2 架构更新，记录单主地图口径、资源分布、木牌兑换、图纸、自建建筑物、物品栏、接口和数据结构。
- `README.md` 只讲游戏本身，不写对标分析和工程审计内容。



## MVP 目标锁定
- 地图规模：只做一个主世界地图，包含木屋、旁友森林、河流、河水、空地游戏机和不可到达远景；允许在这张地图中摆放不同建设元素。
- 核心循环：木屋出发 -> 森林/河岸/河水收集基础材料 -> 木屋前木牌兑换成长物资并领取图纸 -> 建设与布置 -> 建设 3 个物品后开启受控自建建筑物 -> 发现游戏机菜单入口 -> 自主选择像素小游戏 -> 回到主世界 -> 木屋贴纸墙更新。
- 情绪能力：欢笑冲刺、安静漂浮、激动高跳。
- 玩家行为：适当引导的自由探索，不使用任务列表驱动收集；木牌兑换、建设物需求、场景缺口和材料反馈承担引导作用。
- 资源分布：木材、石子、花种、河贝和鱼来自森林、河岸或河水；表情碎片、旧卡带、星屑灯芯和贴纸主要通过木屋前木牌用物资兑换。
- 图纸机制：木牌赠送建设物图纸；初始赠送木桥、花圃、林间路牌；后续按修桥、小游戏完成和建设数量赠送新图纸。
- 自建机制：建设 3 个图纸物品后开启受控自建建筑物，首版只支持 1x1 或 2x2 小型外观组合，不做完整体素沙盒。
- 物品栏：物品栏格子数必须等于物品种类 + 3；v2 初始 9 类物品，因此为 12 格；每类物品放置上限 9999。
- 家园建设：木屋周围优先支持花圃、木栅栏、工作台、贴纸墙、河岸灯、游戏机底座、林间路牌等小型建设物。
- 时间调节：保留清晨、午后、夜晚三段可调时间，先影响光照与提示可见性，不影响复杂产出概率。
- 存档：桌面版本地 JSON，网页演示 `localStorage`，暂不接入服务器数据库。
- 不做内容：大地图、完整 NPC 社交、复杂建造、战斗、联网、账号系统、商业化系统。



## 阶段规划



### 阶段 1：规划与规范
- 任务清单：
  - 读取 `CLAUDE.md` 与 `AGENT_EXECUTION_PROTOCOL.md`。
  - 更新游戏设计规划、关卡草图、项目结构和执行计划。
  - 补充参考研究与三轮对标审核标准。
- 验收标准：
  - 根目录存在 `CLAUDE.md`、`implementation_plan.md`、`AGENT_EXECUTION_PROTOCOL.md`、`diff.md`、`notes.txt`。
  - `doc/design_document.md` 覆盖玩法、画面、接口、数据结构、本地存档和技术架构。
  - `doc/level_sketches.md` 覆盖场景草图、玩家动线、节点触发条件、反馈和数据变化。



### 阶段 2：Unity 工程与核心系统
- 任务清单：
  - 将 Unity 6 URP 工程并入仓库 `unity/`。
  - 实现 `PlayerState`、`ItemCatalog`、`InventoryService`、`SignboardService`、`BlueprintService`、`BuildService`、`CustomBuildService`。
  - 建立 EditMode 自动测试，覆盖物品栏 9+3=12、单项 9999、木牌兑换、建设 3 个物品后开启自建。
  - 每实现一个服务后运行最小测试；测试未过不继续扩展功能。
- 验收标准：
  - Unity BatchMode EditMode 测试能生成结果文件且失败数为 0。
  - 核心服务不依赖场景临时对象，能被后续 UI 和场景节点复用。
  - 代码与 `PROJECT_ARCHITECTURE_UPDATE.md` 的接口和数据结构一致。



### 阶段 3：主世界单地图可玩骨架
- 任务清单：
  - 创建 `WorldHub` 单主地图：木屋、木牌、森林、河岸、河水、半损坏木桥、空地游戏机、不可达远景光幕。
  - 实现主角移动、靠近提示、森林/河岸/河水采集、木牌交互入口。
  - 实现清晨、午后、夜晚三段视觉状态。
  - 用占位素材先跑通空间、比例和反馈，不提前追求最终美术。
- 验收标准：
  - 新玩家能在同一张主世界地图内理解木屋、森林、河岸、木牌和游戏机位置关系。
  - 采集资源后物品栏状态变化可见。
  - 每个节点都有触发条件、反馈和状态写入。



### 阶段 4：建设、图纸和自建闭环
- 任务清单：
  - 木屋前木牌可打开兑换、图纸赠送和小镇记录入口。
  - 物品栏按物品种类 + 3 计算格子数，单项上限 9999。
  - 木桥、花圃、林间路牌等初始图纸可建设。
  - 建设 3 个图纸物品后开启受控自建建筑物。
  - 自建建筑物支持外形、材料主题和 1x1/2x2 尺寸的受控组合。
- 验收标准：
  - 木牌兑换、图纸赠送、建设和自建解锁均有测试或手动验证记录。
  - 自建建筑物写入 `placedBuildings`，不是只存在于场景临时对象。
  - 木牌 UI 只显示可换、已发现和小镇记录，不退化成任务列表。



### 阶段 5：像素小游戏与回收奖励
- 任务清单：
  - 旧卡带解锁游戏机入口。
  - 像素小游戏可收集 3 个贴纸、切换情绪能力、返回主世界。
  - 完成后贴纸墙更新、木牌赠送后续图纸并保存进度。
- 验收标准：
  - 新玩家能在低压力引导下看懂第一轮闭环，不把 5 分钟作为强制节奏或任务限制。
  - 不出现卡死、无法返回、奖励丢失或存档损坏。
  - 每个交互都有视觉或声音反馈。
  - 失败不扣资源，返回主世界后游戏机和贴纸墙都有可见变化。



### 阶段 6：桌面打包、自动构建和质量审核
- 任务清单：
  - 更新自动测试和构建程序，支持 Unity BatchMode EditMode 测试、PlayMode 冒烟测试和 Windows x64 构建。
  - 完成 Windows x64 构建验证；macOS Universal 保留同工程配置，等 macOS 环境再打包。
  - 做代码 review、性能初查、状态存档检查和 80 分视觉/体验门禁。
- 验收标准：
  - 自动测试通过，构建产物可启动。
  - 采集、兑换、建设、游戏机菜单入口和贴纸墙回收链路可完成，不强制固定时间。
  - 未出现新的 Unity Console 编译错误。
  - 最终阶段结束后的 git 提交和推送必须等待用户同意。



### 阶段 7：网页概念演示最终对齐
- 任务清单：
  - 在 Unity 核心系统和画面方向稳定后，再更新 `index.html`、`style.css`、`app.js`。
  - 网页只作为最终概念展示，不反向主导 Unity 设计。
  - 同步展示木牌兑换、物品栏 12 格、基础材料、图纸、自建解锁、游戏机和贴纸墙回收。
- 验收标准：
  - 直接打开 `index.html` 可运行，无控制台阻断错误。
  - 页面能表达 Unity 已确定的主世界关系和像素模式差异。
  - 网页演示不引入 Unity 中不存在的核心玩法。



### 阶段 8：Unity 视觉可玩版
- 任务清单：
  - 新增 `doc/unity_visual_playable_guide.md`，作为新手打开 Unity、查看资产、运行验证、AI 素材生成、对标审核和 Codex 实现顺序的执行手册。
  - 新增 `doc/visual_playable_art_direction.md`，确认用户提供的主角图为阶段 8 视觉基准，补齐主角、地图、资产、UI、提示词和验收标准。
  - 新增 `doc/visual_benchmark_review_stage8.md`，把用户补充的动森/Minecraft 参考图和公开对标研究拆成森林、河流、主角、木屋、游戏机、交互路径和二级验收标准。
  - 将 `assets/reference/protagonist/aethel_ocean_oracle_protagonist_concept.png` 作为内部概念参考，并在 `assets/licenses/asset_sources.md` 记录来源、用途、SHA256 和发布前复核要求。
  - 将用户补充的 3 张视觉对标图归档到 `assets/reference/visual-benchmark/`，仅用于内部方法分析，禁止复制参考素材或 UI。
  - 建立 `Assets/Prefabs/` 与 `Assets/Materials/` 的世界、建设、小游戏目录，先用 Unity 基础几何体和 URP Lit/Emission 材质生成低成本可替换资产。
  - 升级 `WorldHubSceneBuilder` 和 `WorldHub.unity`，让木屋、木牌、旁友森林、河流、河岸、半损坏/已修复木桥、空地游戏机和不可到达远景光幕在 1080p 截图中一眼可读。
  - 新增运行态薄视图层：`GameStateRunner`、`PromptView`、`InventoryView`、`SignboardView`、`BuildMenuView`、`BuildPlacementView`、`StickerWallView`、`ArcadeMachineView`。
  - 让场景反馈从纯数值变成可见变化：采集数字增加、材料不足短提示、建设空位替换成建设物、修桥模型切换、游戏机屏幕发光、贴纸墙显示新增贴纸。
  - 阶段 8A 操作方式锁定：`WASD/方向键` 移动，`E` 互动，木牌面板 `1-4` 兑换，游戏机菜单 `Enter` 进入像素小游戏，小游戏收齐 3 个贴纸后按 `E` 从出口回家。
  - 阶段 8B 新增“清理后建设”流程：木屋前或森林边缘的落枝/碎石/杂草先作为可清理障碍，清理后给予基础材料并转换为花圃建设空位；花圃建设仍走 `BuildService` 和可持久化状态。
  - 保持服务层为唯一业务规则来源，UI 和场景只调用 `GameState`、`WorldNodeService`、`SignboardService`、`BuildService`、`CustomBuildService`、`MiniGameService`。
  - 每完成一个视图或场景小功能，运行最小测试；阶段完成运行 `tools/run-stage6-validation.ps1`。
- 验收标准：
  - 新手按 `doc/unity_visual_playable_guide.md` 能打开 Unity Hub、找到 `Assets/Scenes/WorldHub.unity`、点击 Play、找到 Project/Hierarchy/Inspector/Console，并知道如何查看 Prefab 和材质。
  - 主角视觉按用户提供概念图落地：长黑发、紫黑服装、羽饰/贝壳点缀、漂浮灵鱼、安静/欢笑/激动三表情可读；阶段 8 可用低模代理体或 Sprite/Card，不要求完整角色建模。
  - `WorldHub` 截图能明确表达单主地图家园：木屋、木牌、森林、河水、桥、建设空位、游戏机、远景光幕全部可读。
  - 画面方向吸收动森和星露谷的生活节奏与资源循环方法，但朝 Unity URP 半写实高清低模推进，不复制任何参考游戏素材或 UI。
  - 12 格物品栏、木牌三块信息、建设菜单和短提示 UI 像生活工具和小镇记录，不像任务列表或后台表格。
  - 建设成功必须产生场景对象变化；失败分支不扣材料，并有短提示。
  - 游戏机是发现后的菜单入口，旧卡带满足后玩家自主进入像素小游戏；进入后规则变化为收集 3 个贴纸和出口返回。
  - 贴纸墙回收可见，完成小游戏后回到主世界能看到贴纸墙变化。
  - 用户可以不修改代码，直接在 Unity Play Mode 中按推荐体验路径完成“采集 -> 兑换 -> 建设 -> 游戏机 -> 回家展示”的自由探索闭环。
  - 玩家必须能完成至少 1 条“清理杂草/碎石/落枝 -> 获得基础材料 -> 空地变为可建设花圃 -> 建设花圃”的可见链路；清理前不能直接建设，清理和建设失败均不能错误扣材料。
  - 阶段 8B 画面升级必须按 `doc/visual_benchmark_review_stage8.md` 二级验收达到 80 分以上：主角辨识度、森林边缘、河流与桥、材料可读性、生活 UI 和电子入口均需通过。
  - 不引入新技术栈、不新增联网/账号/抽卡/战斗系统、不扩展多个主世界地图。
  - EditMode、PlayMode、Windows 构建和构建产物启动冒烟均通过。
  - 按 `doc/unity_visual_playable_guide.md` 的 80 分审核表自评达到 80 分以上，低于 80 分先修视觉和反馈，不新增玩法。



### 阶段 8C：视觉/物理一致性与角色升级
- 目标：
  - 把阶段 8B 的“能看懂、能跑通”提升到“画面边界、玩家碰撞和交互反馈一致”。当前用户已指出的缺陷必须作为 8C 的第一优先级：主角不能越过上方地图边界，不能进入河道空隙，未修桥不能过河，修桥后必须能通过桥。
  - 主角不再停留在几何集合代理，进入 2.5D 角色牌或更细低模代理方案，朝用户 Aethel 概念图和动森式强轮廓靠近。
- 对标调研与方法：
  - 动森官方页 `https://animalcrossing.nintendo.com/new-horizons/create/`：学习岛屿生活、家园建设、DIY/装饰和生活化入口的方法，不复制 UI 或素材。
  - Minecraft 官方页 `https://www.minecraft.net/en-us/about-minecraft`：学习探索、材料、建造和强形状反馈的方法，确保资源来源与建设结果因果清楚。
  - Unity 官方物理/碰撞资料作为技术方向：阶段 8C 先用 deterministic walkable-area 判定修复原型，阶段 9 再升级到 Collider/Trigger 或 NavMesh 边界，避免现阶段用复杂物理破坏可测性。
- 实现拆分：
  - 8C-1 边界模型：把可玩区域从简单 `x/z` 钳制改为“陆地区域集合 + 河道禁入带 + 桥通行走廊”。每个视觉地块在 `WorldHubSceneBuilder` 中必须有对应的可行走矩形或阻挡矩形记录。
  - 8C-2 过河规则：`PlayerController` 先判断目标点是否在河道；未修桥时所有河道点不可进入；修桥后只开放桥面走廊。桥面走廊要比视觉桥稍宽，避免玩家在桥边缘被卡住。
  - 8C-3 边界回归测试：新增 EditMode 测试覆盖“上方越界被挡住、右侧岛屿上沿被挡住、河流缝隙会被推出、未修桥不进河道、修桥后桥位可通过”。
  - 8C-4 主角升级：先做 2.5D 竖版角色牌或更细低模组合，必须包含大头/长黑发/紫黑衣/白羽或贝壳头饰/漂浮灵鱼/三表情 HUD。角色在 64px 缩略图仍要能看出轮廓。
  - 8C-5 交互反馈：采集、清理、建设、修桥、打开背包、打开木牌、游戏机启动、收贴纸和回家展示均至少有一种反馈：模型显隐、短提示、闪光、屏幕发光或简单位移动画。现阶段不强制完整骨骼动画。
  - 8C-6 背包/木牌 UI：把 IMGUI 原型拆成可替换的 Unity UI 目标规格，样式为大圆角容器、图标网格、生活记录文字，不做任务清单。
- 调试流程：
  1. 每修一个移动/碰撞规则，先写或更新 `PlayerControllerTests`，只跑 `tools\run-unity-editmode-tests.ps1`。
  2. 每改一次 `WorldHubSceneBuilder`，跑 `tools\run-unity-scene-builder.ps1`，再打开 `WorldHub.unity` 目测视觉和物理边界是否一致。
  3. 每完成一个可试玩切片，跑 `tools\run-stage6-validation.ps1`，生成 Windows 构建。
  4. 用户试玩该切片并提出截图/建议；Codex 记录到 `notes.txt` / `diff.md`，先修阻断问题，再进入下一个切片。
- 用户试玩口：
  - 8C-1/8C-2 完成后：用户只测移动、越界、河道、桥通行，不测新增玩法。
  - 8C-4 完成后：用户只测主角辨识度、比例、表情和灵鱼，不新增系统。
  - 8C-5/8C-6 完成后：用户测采集/清理/建设/UI 反馈是否像生活工具。
- 验收标准：
  - 玩家无法进入视觉水体、无法走出视觉小岛、不会卡在桥边缘；修桥后能稳定从桥通过。
  - 主角代理不再像纯几何块堆叠，能体现 Aethel 轮廓和动森式可爱比例。
  - 截图 review 画面验收达到 90 分以上；交互验收保持 95 分以上。
  - 阶段完成必须通过 EditMode、PlayMode、Windows 构建和启动冒烟。



### 阶段 9：对标快照、试玩回路与发布级视觉打磨
- 目标：
  - 建立稳定的“截图 -> 对标 -> 评分 -> 修复 -> 用户试玩 -> 再评分”流程，把主世界从原型可玩推进到接近动森式画面/物理一致性和生活反馈水平。
- 对标资产与快照：
  - 参考原始目录：`C:\Users\72440\Desktop\小游戏`。
  - 仓库归档目录：`assets/reference/visual-benchmark/`、`assets/reference/protagonist/`。
  - 每次 review 固定输出 3 张 Unity 快照：`WorldHub_1080p_overview`、`WorldHub_bridge_and_river`、`WorldHub_inventory_or_signboard`。必要时用 Chrome MCP/截图工具记录网页或构建画面，但 Unity 是主实现，网页不反向主导。
- 评价体系：
  - 画面 100 分：主角 15、家园锚点 15、森林花园 15、河流/桥/浅滩 15、非矩形边界 10、生活 UI 10、电子入口 10、色彩/镜头 10。
  - 物理 100 分：边界一致 20、水体禁入 20、桥通行 20、交互半径 15、建设替换 15、无卡死/无穿模 10。
  - 交互 100 分：木牌 10、采集 10、清理花圃 15、建设 15、背包 10、游戏机 10、小游戏回收 10、反馈可读性 10、自动验证 10。
  - 任一维度低于 85 分，不进入新玩法；先修画面、物理或反馈。
- 实现拆分：
  - 9-1 主角资产替换：将 8C 代理替换为可维护的 Sprite/Card 或低模 Prefab，保持可替换材质和三表情 HUD。
  - 9-2 场景物理升级：把阶段 8C 的数学边界整理成可视化 `WorldBoundaryDebug` 或 Collider/Trigger 配置，保证设计师能看见哪些区域可走。
  - 9-3 UI 正式化：把背包、木牌、提示气泡从 IMGUI 迁移到 Unity UI Prefab；大圆角、图标网格、操作提示统一样式。
  - 9-4 环境反馈：增加采集/清理/建设/修桥/贴纸墙更新的动画、音效和粒子占位，但仍不引入战斗、抽卡、联网、账号。
  - 9-5 快照自动化：新增截图或构建后手动截图清单，把每次评分结果写入 `doc/visual_benchmark_review_stage8.md` 或阶段 9 review 文件。
- 调试与试玩节奏：
  1. Codex 完成一个 9.x 切片并跑最小测试。
  2. Codex 生成或指引生成固定快照，按评分表给自评和扣分项。
  3. 用户试玩同一个构建并提出建议。
  4. Codex 只修本轮建议和评分低项，不顺手新增玩法。
  5. 通过后本地提交并正常推送；规划 plan 最终阶段结束的 git 仍按用户确认口径执行。
- 验收标准：
  - 玩家看到的地形、水体、桥、边界与实际物理行为一致。
  - 主角、森林、河流、背包、木牌和游戏机在 1080p 快照中达到可公开 review 的视觉完整度。
  - 阶段完成后 `tools\run-stage6-validation.ps1` 通过，且用户完成至少一次试玩反馈回路。



## 每 10 分钟 Review 规则
- Review 玩法：是否仍围绕“小地图闭环”，有没有无意义扩张。
- Review 单地图：是否仍是一个主世界地图内的系统深度，而不是扩成多个主世界场景。
- Review 画面：是否能清晰看出木屋、森林、河流、游戏机入口和像素差异。
- Review 对标：动森对标家园生活动机和温和引导；我的世界对标材料-建设反馈和可读资源；星穹铁道对标电子入口、远景光幕和进入后规则变化。若实现偏离这些方法，先修设计再继续。
- Review UI：界面是否像生活工具和小镇记录，不像任务清单或后台表格；物品栏、木牌和建设界面是否能让玩家自然理解下一步。
- Review 最终体验：玩家是否能在非强制引导下形成“采集 -> 兑换 -> 建设 -> 游戏机菜单入口 -> 回家展示”的完整记忆点。
- Review 实现：当前代码是否能映射到文档里的接口与数据结构。
- Review 物品栏：格子数是否等于物品种类 + 3，单项上限是否保持 9999。
- Review 素材：AI 生成或开源素材是否有来源、许可证、用途记录，并通过统一风格检查。
- Review 记录：每次关键复核写入 `notes.txt` 或 `diff.md`。
- 未通过处理：停止新增功能，先修复偏离点，再继续。



## 三次对标审核通过标准
- 第一次：文档 / PRD / 系分 / 测分对标。通过条件是把用户点到为止的设计补全成可执行规格：玩家行为路径、状态变化、反馈、接口、数据结构、测试点和失败分支必须互相对应。动森对标“家园生活动机、非强制引导、可布置单地图”；我的世界对标“材料来源清晰、材料到建设的因果反馈、放置结果可读”；星穹铁道对标“现代电子入口、远景光幕、进入后规则变化”。任何只复述需求、没有补交互和实现细节的文档不通过。
- 第二次：画面 / UI / 体验对标。通过条件是 Unity 主世界先达标，后续网页概念页再跟随 Unity 表达同一套标准：一眼能看出木屋家园、木牌兑换、森林/河水资源、建设摆放、游戏机入口、像素模式差异和不可到达现代远景。动森标准是温和、生活化、家园感强；我的世界标准是资源和建设物形状清楚、放置反馈明确；星穹铁道标准是电子屏、光幕和转场有现代感但不压过家园核心。UI 必须像小镇工具和记录，不像任务清单、后台表格或纯网页说明页。
- 第三次：实现 / 架构 / 自动测试对标。通过条件是 Unity 可按场景、脚本、数据、测试直接开工并持续验证：`WorldHub` 单主地图、`InventoryService`、`SignboardService`、`BuildService`、`CustomBuildService`、`MiniGameStateMachine`、`SaveService` 都有明确职责；每个功能完成后有最小测试，阶段完成后跑自动测试和构建验证。若实现无法支持动森式单地图布置、我的世界式材料建设反馈、星穹铁道式入口规则切换，则停止新增功能，先补架构和测试。



## 80 分质量门禁
- 评分文件：`doc/visual_alignment_standard.md`。
- 通过线：100 分制，80 分以上才能进入实现阶段。
- 当前 v1 自评分：87/100，允许进入“实现前需求确认”，但不代表最终美术品质完成。
- 角色审核：资深游戏设计者看玩法动机，资深玩家看可读性和奖励反馈，资深架构师看状态机、接口和可实现性。
- 未通过处理：若任意 review 低于 80 分，先修复文档和概念演示，再进入 Unity 实现。



## 近期执行顺序
- 阶段 A：先把 Unity 工程和核心框架打起来，网页概念演示暂不更新。
- 阶段 B：实现并测试状态模型、资源模型、木牌兑换模型、图纸模型、物品栏模型和建设模型。
- 阶段 C：实现单主地图可移动、采集、木牌、建设、时间切换和游戏机入口。
- 阶段 D：实现建设 3 个物品后的受控自建建筑物、像素小游戏和返回主世界的奖励结算。
- 阶段 E：自动测试、构建、代码 review、体验 review 和 80 分门禁。
- 阶段 F：最后更新网页概念演示，让网页跟随 Unity 已跑通的设计。



## 运行与画质要求
- 新手运行指南：见 `doc/RUN_DEBUG_GUIDE.md`。
- 网页演示：必须无需安装依赖、无需登录、可直接打开 `index.html`。
- Unity 原型：先保证本地 Play Mode 能跑通，不接入账号系统、联网服务或高成本插件。
- 默认画面：2K，推荐 2560x1440。
- 可选画面：1080p，推荐 1920x1080。
- 素材源文件：主要场景和主角概念素材尽量使用 4K；达不到 4K 的素材只作为占位，必须标记待优化。
- 性能要求：优先保证 1080p 稳定运行，再提升 2K；首版避免高性能要求的复杂水体、大量实时灯光、体积雾和重后处理。



## Git 文档策略
- 根目录的执行约束、README、计划和报告可以上传 git。
- `doc/` 下多数详细设计文档默认只保留本地，不上传 git；git 只保留 `doc/README.md` 作为目录索引。
- 需要分享给远端的文档内容，应先迁移到根目录的必要文件或用户明确要求上传的文件。



## Git 与交付计划
- 阶段内实现分支按“实现 -> 最小测试 -> review -> 本地提交 -> 正常推送”的节奏推进。
- 只有规划 plan 的最终阶段结束 git 需要再次等待用户明确同意；其他中间实现阶段按用户本轮授权正常提交和推送。
- 远端保持 `git@github.com:xzgjj/word_game.git`。
- 若网络或 SSH 阻塞导致推送失败，记录失败原因、保留本地提交和分支，不改用破坏性 git 操作。
