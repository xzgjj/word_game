# CLAUDE.md



## 项目简介
基于现代情绪冒险与轻松关卡平台的桌面原生小游戏规划，配套网页概念交互演示。



## 构建与运行指令
- 初始阶段：项目还未完成具体代码，当前文档为规划阶段。
- 未来 Web 概念版：`npm install` / `npm run dev` / `npm run build`
- 未来桌面版：根据引擎选择补充，如 Unity 可使用 `Unity -projectPath .`，Godot 可使用 `godot --path .`



## 代码规范
- 缩进要求：4 空格，不使用 Tab。
- 命名习惯：文件名小写短横分隔；变量和函数使用 `camelCase`；类和组件使用 `PascalCase`。
- 必须使用的库：当前为规划阶段，后续项目初始化时按 `Unity` 或 `Godot` 标准依赖管理。
- 提示词要求：所有 AI 提示词需包含“现代情绪冒险”“关卡式轻松平台”“桌面原生”“网页演示原型”“动态表情包”关键词。



## 常用命令
- 开发服务器：`npm run dev`（Web 原型）
- 构建发布：`npm run build`
- 代码检查：`npm run lint`
- 版本控制：`git status` / `git diff` / `git add .` / `git commit -m "..."`



## 核心约束
- 依据要求生成：桌面原生小游戏 + 网页概念交互演示。
- 数据库迁移必须通过迁移脚本执行（如存在数据库时）。
- 不允许未经用户明确许可删除项目文件、配置文件、依赖锁定文件、数据库文件、用户上传资源。
- 不允许修改核心模块/安全模块、重构整体架构、改变技术栈、扩展问题定义。



## 核心逻辑速览
- `世界 Hub`：社交与关卡入口，呈现情绪交互与任务。
- `表情角色`：动态表情包角色，拥有不同情绪状态能力。
- `关卡玩法`：短小平台与情绪切换解谜，目标收集贴纸/到达终点。
- `网页演示`：概念演示页展示核心玩法和美术风格。



## 安全与代码质量
- 所有用户输入必须经过验证与清洗。
- 提交前必须通过 lint 和测试，确保无已知漏洞。
- 保持内存安全与并发安全原则。
- 代码变更应保持最小修改，避免引入次生问题。



## AI 协作原则
- AI 可建议修改代码，但需明确说明改动位置和原因。
- 重大重构前需与用户讨论并获得确认。
- 代码优化应精准定位，最小干预原则。
- 新增依赖必须说明用途并征得用户同意。
- 复杂功能需提供伪代码或设计思路分析，征确认后再实现。



## 核心参考
- 采用官方文档与 SDK 作为实现准则。
- 参数定义、请求方式、返回值处理需严格遵循官方规范。



## 禁止项
- 未经许可不得删除项目文件、配置文件、依赖锁定文件、数据库文件、用户上传资源。
- 修改前须读取 `AGENT_EXECUTION_PROTOCOL.md` 和 `CLAUDE.md`。
- 任何自动化修改前必须先进行变更前确认并等待批准。
