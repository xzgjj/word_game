# AGENT_EXECUTION_PROTOCOL.md



## 执行规则
1. 在执行任何修改前，必须先读取 `AGENT_EXECUTION_PROTOCOL.md` 和 `CLAUDE.md`，理解约束与上下文。
2. 在动手前必须先输出一个“变更前确认”区块，且仅用中文、简短说明必要原因，格式固定为：
   - GOAL_CONFIRMATION:
   - IMPACT_ANALYSIS:
   - RISK_LEVEL:（低/中/高）
3. 建立目标锁定机制、技术栈锁定、作用域白名单机制。
4. 在执行任何非局部修改前，必须确保 git 工作区干净。自动化修改后测试未全过，或引入新的 Linter 警告，必须立即回滚代码。
5. 修改准则：深度溯源分析原因，最小干预原则；优化需要确立核心问题，给出方法以及核心参数调参命令/要点。
6. 新增功能要求：理解原有业务逻辑，依据要求评估方案，避免影响原业务，保持一致性。
7. 输出上述确认后，必须停止并等待用户明确批准；未获批准不得执行任何修改：获批后低风险可以直接执行。
8. 严格禁止：
   - 修改核心模块/安全模块
   - 重构整体架构
   - 改变技术栈
   - 扩展问题定义
9. 严格禁止自行删除以下文件/数据：
   - 数据库文件
   - 配置文件与密钥
   - 用户上传资源
   - 依赖锁定文件（如 package-lock.json、poetry.lock、Cargo.lock 等）
   - 任何项目文件
   
   

## 执行流程
- 读取 → 分析 → 确认 → 等待批准 → 执行



## 违规处理
- 发现违反规则时，应立即停止并报告当前状态。
- 在未获批准的情况下，不得继续修改。
